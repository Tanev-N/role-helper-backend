httpserver:
  ip: localhost
  port: 8080
  allowed_ips_by_cors:
    - http://localhost

postgres:
  ip: postgres
  port: 5432
  dbname: test
  user: test
  password: test

redis:
  ip: redis
  port: 6379
  dbnum: 0
  user: test
  password: test

